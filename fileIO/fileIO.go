package main

func main() {
	// f, err := os.Open("ex.json")

	// if err != nil {
	// 	fmt.Println(err.Error())
	// 	panic(err)
	// }
	// fileInfo, err := f.Stat()

	// if err != nil {
	// 	panic(err)
	// }
	// fmt.Println(fileInfo.IsDir(), fileInfo.Size())

	// read file

	// f, err := os.Open("ex.json")

	// println()

	// if err != nil {
	// 	panic(err)
	// }

	// buf := make([]byte, 10)

	// d, err := f.Read(buf)

	// if err != nil {
	// 	panic(err)
	// }
	// println(buf, d)

	// for i := 0; i < len(buf); i++ {
	// 	println(string(buf[i]))
	// }

	// defer f.Close()

	// data, err := os.ReadFile("ex.json")

	// if err != nil {

	// }

	// fmt.Print(string(data))

	// read folders

	// dir, err := os.Open(".")

	// if err != nil {
	// 	panic(err)
	// }

	// defer dir.Close()

	// fileInfo, err := dir.ReadDir(-1)

	// for _, fi := range fileInfo {
	// 	println(fi.Name())
	// }

	// create file

	// f, err := os.Create("example.txt")
	// if err != nil {
	// 	panic(err)
	// }
	// defer f.Close()

	// f.WriteString("GO...hello from chai")

	// // overwrite the file
	// bytes := []byte("hello GOLang")

	// f.Write(bytes)

	// streaming -----
	

}
